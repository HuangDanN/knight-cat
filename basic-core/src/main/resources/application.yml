server:
  port: 9100
spring:
  application:
    name: ${artifactId}
  datasource:
    url: jdbc:p6spy:mysql://localhost:3306/basic-system
    username: root
    password: huang
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
  web:
    resources:
      add-mappings: false
  mvc:
    throw-exception-if-no-handler-found: true
  profiles:
    include: mybatis, saToken